class Solution:
    def arrayStringsAreEqual(self, word1: List[str], word2: List[str]) -> bool:
        d = "".join(word1)
        e = "".join(word2)
        if d == e:
            return True
        else:
            return False
