class Solution:
    def sortArrayByParity(self, nums: List[int]) -> List[int]:
        s = []
        k = []
        for i in nums:
            if i % 2 == 0:
                s.append(i)
            else:
                k.append(i)
        for i in k:
            s.append(i)
        return s
