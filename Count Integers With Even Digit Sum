class Solution:
    def countEven(self, num: int) -> int:
        l = 0
        for i in range(1,num+1):
            nums = list(str(i))
            if sum(int(k) for k in nums) % 2==0:
                l += 1
        return(l)

                    
        
